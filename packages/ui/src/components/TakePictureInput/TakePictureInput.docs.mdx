import { Meta, Canvas } from '@storybook/blocks';
import * as Stories from './TakePictureInput.stories';

<Meta of={Stories} />

# TakePictureInput

Komponen untuk mengambil foto dengan bantuan pose tangan. Berikut preview dengan tinggi canvas yang diperbesar agar modal terlihat penuh di halaman Docs.

## Basic
<Canvas of={Stories.Basic} height={720} />

## With Label
<Canvas of={Stories.WithLabel} height={720} />